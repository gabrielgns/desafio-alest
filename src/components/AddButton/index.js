import React from 'react';
import { ButtonWrapper, AddIcon } from './styles';

const AddButton = () => {
  return (
    <ButtonWrapper>
      <AddIcon />
    </ButtonWrapper>
  );
};

export default AddButton;
